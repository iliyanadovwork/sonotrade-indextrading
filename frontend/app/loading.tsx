import { SXPageLoading } from '@/components/sx/SXPageLoading'

// App Router loading UI — shown during navigation between pages while the
// destination route renders. Uses the same glyph spinner as the rest of the site.
export default function Loading() {
  return <SXPageLoading />
}
