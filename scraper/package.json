{
  "name": "sonotrade-scraper",
  "version": "1.0.0",
  "private": true,
  "description": "Daily Spotify monthly-listeners scraper → artist index pipeline (Supabase)",
  "type": "module",
  "scripts": {
    "scrape": "tsx src/run.ts",
    "dry-run": "DRY_RUN=1 tsx src/run.ts",
    "test": "node --import tsx --test src/index-math.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^20.0.0"
  }
}
